@model FurnitureShop.Models.Address

@{
    ViewBag.Title = "EditAddress";
}

<h2>EditAddress</h2>
@using (Html.BeginForm()) {
@Html.EditorForModel(Model)
<div class="editor-float-none">
    <p>
         @Html.ActionLink("Cancel", "MyAccount")
        <input type="submit" value="Update address" class="actionButtons" />
    </p>
</div>
}