@model IEnumerable<string>

@*Html.ActionLink("Home", "List", "Products")*@
@foreach (var link in Model)
{
    <li>@Html.RouteLink(link, new
{
    controller = "Products",
    action = "List",
    category = link,
    page = 1
})</li>
}